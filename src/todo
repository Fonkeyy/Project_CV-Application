set all social media logo same size
configure sending message form
see keyboard navigation on info section
choose new color for header job title?
add choice for title color font? input color?

See why SocialBtn !isSubmitted after adding a new social media from list in SocialModal