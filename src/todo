change font header
set all social media logo same size
configure sending message form
check btn wrapper on tiles
see keyboard navigation on info section
add white variation to input backgrond
add persistance in tile textarea 